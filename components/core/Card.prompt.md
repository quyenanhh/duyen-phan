Default surface for every panel, list and form group.

```jsx
<Card title="Doanh thu hôm nay" subtitle="Tất cả chi nhánh" actions={<IconButton icon="ellipsis" label="Tuỳ chọn" size="sm" />}>
  …
</Card>
```
Never stack shadows or use a heavier shadow than `--shadow-raised`.
