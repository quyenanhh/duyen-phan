Zero-state for filtered lists and new branches.

```jsx
<EmptyState icon="receipt-text" title="Chưa có đơn nào hôm nay"
  description="Đơn mới sẽ xuất hiện ở đây ngay khi khách đặt." action={<Button icon="plus">Tạo đơn thủ công</Button>} />
```
